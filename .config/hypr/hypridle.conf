general {
  lock_cmd = pidof hyprlock || hyprlock
  before_sleep_cmd = hyprlock --no-fade-in
  after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'
}

source = ~/.config/hypr/machine-specific/$HYPR_HOST/hypridle.conf
